name: lint

# Kiểm tra style, không build sản phẩm. Ba job độc lập chạy song song:
#   cpp-format  — clang-format cho core/ platform/ client/ (theo .clang-format)
#   kotlin      — ktlint cho tầng UI Kotlin (theo .editorconfig)
#   android     — Android Lint (gradlew lint) trên module app
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  cpp-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: clang-format check
        run: |
          files=$(find core platform client -type f \
            \( -name '*.h' -o -name '*.hpp' -o -name '*.cpp' -o -name '*.cc' -o -name '*.c' \))
          echo "$files"
          clang-format --dry-run --Werror $files

  kotlin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ktlint
        run: |
          curl -sSLO https://github.com/pinterest/ktlint/releases/download/1.5.0/ktlint
          chmod +x ktlint
          sudo mv ktlint /usr/local/bin/

      - name: ktlint check
        run: ktlint "client/android/**/*.kt"

  android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client/android
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Android Lint
        run: |
          chmod +x ./gradlew
          ./gradlew lint --stacktrace

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-lint-report
          path: client/android/app/build/reports/lint-results-*.html
          if-no-files-found: ignore
